# apiVersion: networking.istio.io/v1alpha3
# kind: VirtualService
# metadata:
#   name: kraken-config
# spec:
#   gateways:
#     - mesh
#   hosts:
#     - mykraken-config.kraken-config # Internal host if accessed via Krakend or Istio Mesh
#   http:
#     - route:
#       - destination:
#           host: kraken-config.kraken-config
#           port:
#             number: 5000
