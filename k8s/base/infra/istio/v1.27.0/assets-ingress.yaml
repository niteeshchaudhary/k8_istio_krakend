apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: istio-assets-ingress
  namespace: cluster-infra
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: istio-ingressgateway
                port:
                  number: 80
