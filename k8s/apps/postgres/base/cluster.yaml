apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-cluster
spec:
  instances: 2
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: appuser
      owner: appuser
      secret:
        name: shared-db-secret
